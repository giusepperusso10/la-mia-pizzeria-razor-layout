@{
    Layout = null;
}

<html lang="en">
<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <title>Hello, world!</title>
</head>
<body>

    <div>

        <p>

            <strong>

                1.Dichiarare una variabile valorizzandola con il nostro nome ed un altro con il nostro cognome. Stampare questo valore all'interno di un tag h2'

            </strong>

        </p>



        @{

            string nome = "Giuseppe";
            string cognome = "Russo";

        }

        <h2>@nome @cognome</h2>
    </div>

    <div>

        <p>

            <strong>

                2.Creare una funzione che sommi i numeri da 1 a n (n passato come parametro e n incluso)

            </strong>

        </p>

        @{

            int Somma(int n)
            {
                int somma = 0;
                for (int i = 0; i <= n; i++)
                {


                    somma = somma + i;


                }
                return somma;
            }

        }

        <span>@Somma(4)</span>

    </div>

    <div>

        <p>

            <strong>


                3. Contare quante volte la lettera a è presente nalla frase "nel mezzo del cammin di nostra vita" e stampare questo valore in pagina

            </strong>

        </p>
        @{

            
            string frase = "Nel mezzo del cammin di nostra vita";

            int count = frase.Count(lettera => lettera == 'a');

        }

        <span>La lettera a è presente @count volte</span>

    </div>

    <div>

        <p>

            <strong>

                Creare una List int di 20 numeri interi (generati random da 1 a 90, 1 e 90 inclusi) e mostrare in pagina solo i numeri compresi tra 20 e 70


            </strong>

        </p>

        @{

            List<int> listaInteri = new List<int>();

            Random random = new Random();

            for (int i = 0; i < 20; i++)
            {

                int randomNumber = random.Next(1, 91);
                listaInteri.Add(random.Next(randomNumber));

            }
        }

        <ul>

            @foreach (int number in listaInteri)
            {

                @if (number > 20 && number < 70)
                {

                    <li>@number</li>

                }

            }

        </ul>

    </div>

    <div>

        <p>

            <strong>

                5. Stampare in pagina la data e l'ora attuale usando dateTime con il formato anno-mese-giorno ore:minuti:secondi

            </strong>


        </p>

        @{

            var dataAttuale = DateTime.Now;

        }

        <span> @dataAttuale</span>

    </div>

    <div>

        <p>

            <strong>

                6a.Stampare in pagina la data di 7 giorni fa (usando la classe DateTime)

            </strong>

        </p>

        @{

            var dataDomani = dataAttuale.AddDays(-7);

        }

        <span> @dataDomani.ToString("dd-MM-yyyy")</span>

    </div>

    <div>

        <p>

            <strong>

                6b.Stampare che giorno della settimana è oggi

            </strong>

        </p>


        <span> @dataAttuale.ToString("dddddddd")</span>

    </div>

    <div>

        <p>

            <strong>

                6c. che giorno della settimana sarà nello stesso giorno e mese di oggi ma tra un anno (il risultato dovrà essere il nome del giorno (Lunedì, Martedì, ...)

            </strong>

        </p>

        @{

            var dataTraUnAnno = dataAttuale.AddYears(1);

        }

        <span> @dataTraUnAnno.ToString("dddddddd")</span>
        <span> @dataTraUnAnno.ToString("dd-MM-yyyy")</span>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js" integrity="sha384-QJHtvGhmr9XOIpI6YVutG+2QOK9T+ZnN4kzFN1RtK3zEFEIsxhlmWl5/YESvpZ13" crossorigin="anonymous"></script>

</body>
</html>

